{
  "$schema": "https://codesandbox.io/schemas/tasks.json",
  "setupTasks": ["yarn add @swipelux/connect@latest @swipelux/connect-siwe-next-auth@latest", "yarn build"],
  "tasks": {
    "install-dependencies": {
      "name": "Install Dependencies",
      "command": "yarn add @swipelux/connect@latest"
    },
    "start-app": {
      "name": "Run Dev Server",
      "command": "yarn dev",
      "runAtStart": true
    }
  }
}